{
  "scripts": {
    "serve": "npx @11ty/eleventy --watch --serve",
    "debug": "DEBUG=*Data* npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "quiet": "npx @11ty/eleventy --watch --quiet --serve"
  },

  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-navigation": "^1.0.5",
    "clean-css": "^5.3.3",
    "eleventy-plugin-purgecss": "^0.6.0"
  },
  "devDependencies": {
    "html-minifier": "^4.0.0",
    "terser": "^5.46.1"
  }
}
